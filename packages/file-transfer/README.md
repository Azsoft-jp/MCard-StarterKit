# File transfer modules

FILE transfer is treated as a replaceable module.

Current public modules:

- generic frame builder
- local transfer simulator
- ACK retry simulator
- MoniCard-like sample command naming through profile data

Future modules should be added as profile-scoped plugins rather than hard-coded behavior.
