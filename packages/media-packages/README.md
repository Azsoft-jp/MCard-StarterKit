# Media package modules

Media package handling is profile-neutral.

Current public format:

- `mcard-starterkit.media-package.v1`

Future device-specific media converters should live in separate modules and declare their target profile explicitly.
