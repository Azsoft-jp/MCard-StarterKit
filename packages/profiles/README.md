# Profiles

Device and protocol behavior is kept outside the core as profiles.

Included examples:

- `examples/profiles/monicard-like.sample.json`

Profiles may define categories, command IDs, frame modes, notify parsers, and transfer behavior. Public sample profiles must not contain vendor cloud endpoints, official assets, captured secrets, or extracted application code.
